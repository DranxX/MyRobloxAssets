-- Property Of @TheDranxX
local Revoke = require(game.ReplicatedStorage:WaitForChild("Repok")) -- reference
local part = Revoke.get("Workspace.PartContoh") -- use the method
if part then
	part.Transparency = 0.5
	print("Success")
else
	print("Gagal")
end

-- Property Of @TheDranxX

-- You can also obfuscated this code with prometheus for VM Custom System obfuscator or other
